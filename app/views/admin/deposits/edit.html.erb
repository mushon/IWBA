<%= render 'tab' %>

<h3>
  Edit # <%= @deposit.id %>
</h3>
<div class="row">
  <div class="col-md-8">
    <%= render 'form' %>
    <%= link_to "Delete", admin_deposit_path(@deposit), :method => :delete, :class => "btn btn-danger", :data => {:confirm => "Are you sure?"} %>
  </div>
  <div class="col-md-3">
    <%= render 'aside' %>
  </div>
</div>